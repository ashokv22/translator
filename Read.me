First install all the required packages and libraries. which are listed in requirements file. Run below code to install,
pip install -r requirements.txt

After successful installation of libraries. Run the below code to start translation.
python main.py

To translate the text based on custom text, change text assigned to src_text within doublw quotes.
src = ""




********************************from Ashok************************************